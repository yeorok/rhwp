# Task 41 Step 3 Completion Report: DIFF-2,3,4,6,8 Fixes

## Fix Details

### DIFF-2: Correct CharShape ID Assignment (Severity: Medium)

**Problem**: Cell paragraph char_shapes were empty, causing default value (id=0) substitution during serialization
**Fix**: Added default CharShapeRef (start_pos=0, char_shape_id=0) when char_shapes is empty in cell paragraph correction code

```rust
if cp_para.char_shapes.is_empty() {
    cp_para.char_shapes.push(CharShapeRef {
        start_pos: 0,
        char_shape_id: 0, // Default CharShape
    });
}
```

**Effect**: All cell paragraphs have explicit CharShapeRef. PARA_CHAR_SHAPE record contains accurate data during serialization.

### DIFF-3: Correct ParaShape ID Assignment (Severity: Low-Medium)

**Problem**: Complex logic reusing the first table cell's para_shape_id from existing documents returned wrong IDs
**Fix**: Simplified to use default "body" ParaShape (id=0)

```rust
// Before: Complex logic searching existing table cells (13 lines)
// After:
let cell_para_shape_id: u16 = 0;
```

**Effect**: All cell paragraphs use default body ParaShape. Valid references guaranteed.

### DIFF-4: BorderFill ID (Severity: Medium)

**Status**: Already handled correctly

`create_border_fill_from_css()` correctly returns 1-based IDs, and surgical insert maintains DocInfo raw_stream consistency.

### DIFF-6: Cell LineSeg Metrics (Severity: Low)

**Status**: Already handled correctly in correction code

- `tag = 0x00060000` (bit 17,18 = normal HWP cell paragraph pattern)
- `segment_width = cell_width - left_padding - right_padding`
- `line_height`, `text_height`, `baseline_distance`, `line_spacing` calculated based on font size

### DIFF-8: Table Container PARA_LINE_SEG (Severity: Low)

**Status**: Already handled correctly in table paragraph generation code

- `line_height = total_height`, `text_height = total_height`
- `segment_width = total_width`
- `tag = 0x00060000`

## Integration Test Verification

`test_parse_table_html_save`: Generated 2x2 table (with empty cells) via parse_table_html() -> inserted into existing HWP -> saved -> re-parsed

| Item | Verification Result |
|------|-------------------|
| DIFF-1 empty cell | cells[1],cells[2]: cc=1, text='', has_para_text=false |
| DIFF-2 CharShape | All cells cs=1 (explicit CharShapeRef) |
| DIFF-3 ParaShape | All cells para_shape_id=0 |
| DIFF-5 TABLE attr | tbl_rec_attr=0x04000006 |
| DIFF-6 LineSeg | tag=0x00060000, seg_width > 0 |
| DIFF-7 instance_id | 0x7C174784 (non-0) |
| DIFF-8 container | line_height=2000, seg_width=30000 |

## Generated Files

- `output/save_test_parsed_table.hwp`: HWP file with table generated by parse_table_html() inserted -> needs HWP program verification

## Tests

- `test_parse_table_html_save`: Integration test (new)
- Total tests: 476 passed (475 -> 476)

## Modified Files

| File | Changes |
|------|---------|
| `src/wasm_api.rs` | DIFF-2: char_shapes default added, DIFF-3: cell_para_shape_id simplified, integration test added |
