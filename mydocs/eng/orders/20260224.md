# Today's Tasks - February 24, 2026

| No | Task | Status | Notes |
|----|------|--------|-------|
| 157 | TextBox insertion and basic editing | Done | WASM API (create/delete/properties), unified object selection (image/shape), mouse drag creation, 8-direction handle resize, movement (drag/arrow keys), deletion, object properties dialog TextBox tab (margin/vertical alignment), Undo/Redo |
| 160 | TextBox Z-Order reordering | Done | Rust change_shape_z_order_native (swap method) + WASM API + 4 commands (bring to front/bring forward/send backward/send to back) + right-click context menu. New TextBox creation auto-assigns z_order=max+1. Hancom-compatible paragraph reordering not applied (spec unspecified, to be decided after actual testing in next task) |
| 161 | TextBox Z-Order Hancom compatibility verification | Waiting | P0. Test whether Hancom uses z_order field vs document order for rendering. Reintroduce paragraph reordering (reorder_shape_paragraphs_by_z_order) if needed |
| 158 | TextBox border/fill/vertical writing | Waiting | P1. Border line type/width/color, fill (color/gradient/image), vertical writing (English horizontal/vertical), symbol substitution (period -> ring mark etc.), rotation (90-degree units), shape->TextBox conversion (add text to shape/remove TextBox properties), new drawing properties registration |
| 159 | TextBox linking and advanced features | Waiting | P2. TextBox linking (overflow -> auto continuation to next TextBox), unlink, overwrite when target has content, hyperlink attribute set/unset, group/ungroup objects, alignment/distribution |
| 162 | Custom tab stops and tab leader (fill symbol) rendering | Done | Tab position /2.0 scale correction, left/right/center/decimal tab alignment, cross-run tab handling, SVG/Canvas tab leader (solid/dash/dot) rendering, outside/inside page number position odd/even handling, section-to-section page_number_pos/page number continuous inheritance, Docker WASM build cache volume |

## Backlog

| No | Description | Found | Status | Notes |
|----|-------------|-------|--------|-------|
| B-006 | Multi-column editing design (pagination + DocumentPosition extension) | 2026-02-18 | Unresolved | Currently multi-column supports rendering only. For editing: line-level column separation (PartialParagraph), column_index in DocumentPosition, cursor/selection column awareness needed |
| B-007 | Shadow text area editing/saving error | 2026-02-21 | Unresolved | Adding characters to shadow-effect text then saving causes error. Possible mismatch between changed bit positions (bits 8-10/11-12/13/14) and existing raw_data during serialization |
| B-009 | PS/PCL direct generation print engine (Renderer Trait extension) | 2026-02-23 | Unresolved | HWP model -> PS/PCL command 1:1 mapping. Driver-free IPP direct transmission and printing in WASM environment (PoC), mydocs/tech/direct_printing_guideline.md |
