# Today's Tasks - February 14, 2026

| No | Task | Status | Notes |
|----|------|--------|-------|
| 64 | Web font mapping and system font fallback connection | Done | Rust generic_fallback() function for serif/sans-serif auto branching, wasm-bridge.ts measureTextWidth font substitution connection, 8 substitution items added to style_resolver.rs. 485 Rust tests passed, TS build success |
| 65 | TextBox (GSO TextBox) cursor support | Done | CellContext reuse (cell_index=0) for TextBox cursor enter/move/exit. Arrow key TextBox entry (Hancom behavior), hitTest priority fix, border (solid line/HWPUNIT units) and fill (gradient) rendering, Canvas sin/cos fix. 486 Rust tests passed, TS build success |
| 66 | Text+Table simultaneous paragraph rendering fix | Done | pagination.rs places PartialParagraph (start_line=1) after Table. Table renders first, text correctly positioned below table. 487 Rust tests passed, SVG rendering confirmed |
| 67 | rhwp-studio HWP save feature implementation | Done | Ported from web/ project. wasm-bridge.ts exportHwp()/fileName added, file:save command implemented (Blob download), Ctrl+S shortcut mapping. Text editing save file corruption bug fixed (surgical_update_caret modifies only 12 bytes of DocInfo raw_stream). 488 Rust tests passed, WASM/Vite build success |
| 68 | Auto re-render trigger after async web font 2-stage loading | Done | loadWebFonts() returns backgroundDone Promise, main.ts calls refreshPages() on completion. Vite build success |
| 69 | Paper setup top margin/header height correction (per Hancom help) | Done | PageAreas::from_page_def() modified: body_start=margin_header+margin_top, body_end=height-margin_footer-margin_bottom. Same margin calculation as Hancom HWP. 488 Rust tests passed, SVG body start y=94.47px (expected 94.49px) confirmed |
| 70 | Header/footer rendering and page number handling | Done | pagination.rs HeaderFooterRef struct, per-page Header/Footer assignment (Both/Even/Odd), PageNumberPos collection. layout.rs Header/Footer content rendering (Table-containing Footer support), format_page_number (Arabic/Roman/circled). Per-page paragraph index-based Footer assignment fixes Footer overwrite bug. 488 Rust tests passed, SVG export normal |
| 71 | New number start (NewNumber) + auto numbering system completion | Done | AutoNumber bit offset fix (format 8 bits, superscript bit 12), decoration character parsing/serialization, PageNumberPos serialization fix. NewNumber -> assign_auto_numbers counter reset, DocProperties start number initialization. PageContent.page_number + NewNumber(Page) page number reflection. apply_auto_numbers format/decoration applied, format_page_number duplicate function removed. 488 Rust tests passed, WASM/Vite build success |
| 72 | Table page break border/content duplicate rendering fix | Done | Body node clip_rect field added, SVG clipPath/Canvas clip() applied. Intra-row split minimum threshold (10px) introduced to prevent micro partial rows. 488 Rust tests passed, WASM/Vite build success |
| 73 | Paragraph mark display feature | Done | ComposedLine.has_line_break + TextRunNode.is_line_break_end flags added, paragraph mark changed from pilcrow to return symbol (U+21B5), WasmBridge/command/menu/toolbar toggle connection. 488 Rust tests passed, WASM/Vite build success |
| 73+ | Follow-up fix: Empty paragraph mark position + TextBox inline image + shape border | Done | Empty paragraph return symbol placed at left start position, TextBox inline_y initialized to inner_area.y fixing image position, shape border attr bits 0-5=0 (none) skips border rendering. 488 Rust tests passed, web verification completed |
| 74 | Group Shape parsing and rendering | Done | ShapeObject Picture variant added, rendering transformation matrix (affine transform) parsing for accurate scale/position calculation, HorzRelTo::Page body area reference fix. 488 Rust tests passed, WASM/Vite build success |
| 75 | Fix text before table rendering below table | Done | pagination.rs replaced char_offsets-based approach with table CTRL_HEADER vertical_offset-based. When vertical_offset > 0, all text placed before table in placeholder layout. 488 Rust tests passed, WASM/Vite build success, web verification completed |

## Backlog

| No | Description | Found | Notes |
|----|-------------|-------|-------|
| B1 | ~~Text rendering missing in paragraphs containing both text+Table controls~~ | 2026-02-14 | **Resolved in Task 66** |
| B2 | ~~Auto re-render trigger after async web font 2-stage loading~~ | 2026-02-14 | **Resolved in Task 68** |
