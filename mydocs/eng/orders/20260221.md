# Today's Tasks - February 21, 2026

| No | Task | Status | Notes |
|----|------|--------|-------|
| 125 | Font metric DB generation tool | Done | TTF parsing (head/cmap/hmtx/name) -> 582 font metric DB auto-generated. Hangul syllable decomposition compression (cho x jung x jong), WASM built-in metrics eliminates 100% JS bridge calls. Noto CJK included. 571 tests passed, WASM 1.83MB. devel/main merge completed |
| 126 | Canvas DPR scaling | Done | renderScale=zoom x DPR, canvas.style=physical pixels/DPR, scale upper limit 12.0, margin guides ctx.setTransform. 3 files ~25 lines changed. 571 tests passed, WASM build success |
| 127 | measureText LRU caching | Done | Rust-side Vec-based LRU cache (256 entries). cached_js_measure() wrapper caches WASM<->JS bridge call results. Applied to measure_char_width_hwp + measure_hangul_width_hwp (2 sites). 571 tests passed, WASM build success |
| 128 | Bold/Italic font width correction | Done | MetricMatch struct for Bold fallback detection. Regular fallback applies Hancom-style correction (em_size+10)/20. 2 files ~20 lines changed. 571 tests passed, WASM build success |
| 129 | Font substitution chain completeness verification | Done | 4 broken chains fixed (Gulimche -> Gulim-che, Ansangsu-che -> HCR Dotum). Generic fallback regex improved (palatino/georgia/batang/gungsuh). 571 tests passed |
| 130 | Per-document dynamic web font loading | Done | WASM getDocumentInfo() fontsUsed added. font-loader docFonts selective load + loadedFiles dedup. Status bar progress display. 5 files ~40 lines changed. 571 tests passed, WASM build success |
| 131 | Text effect rendering (outline/shadow/emboss/engrave) | Done | outline_type bit parsing fix (hwplib reference, bits 8-10/11-12) + emboss/engrave parsing (bits 13/14). TextStyle extension + style_resolver connection. Canvas multi-pass rendering (shadow/outline/emboss/engrave). 8 files ~125 lines changed. 571 tests passed, WASM build success |
| 132 | Table creation feature | Done | Rust createTable WASM API (insert row x col table at cursor position, paragraph split/empty paragraph replacement support). Hancom-style 8x10 grid picker + "Create Table..." detail dialog (rows/cols, size specification, etc.). Accessible from both menu and toolbar. Hancom blank_h_saved.hwp reference default attribute values (attr/padding/alignment/height etc.). Empty cell caret position fallback improved. 7 files ~750 lines changed. 571 tests passed, WASM build success |
| 133 | New blank document creation + save | Done | Prerequisite: Enter key bug fix (split_at char_count control not included, raw_header_extra lost). Built-in template (blank2010.hwp) based new document creation + document initialization refactoring + file:new-doc command activation. Cell split save corruption fix (control_mask recalculation + has_para_text correction). Save filename input: showSaveFilePicker (Chrome/Edge) priority + custom dialog fallback. Cell split dialog + split_cell_into() implemented. 582 tests passed, WASM build success, TS compile success |
| 134 | CFB error file recovery open | Done | "Malformed FAT (sector 0 pointed to twice)" error file opened normally with LenientCfbReader fallback. parse_hwp() strict -> lenient 2-stage strategy. LenientCfbReader read_file_header/section_count/read_body_text_section_full added. BinData lenient load support. 2 files ~200 lines changed. 582 tests passed, WASM build success |
| 135 | Table cell border resize marker display | Done | Cell border hover shows row-resize/col-resize cursor + blue marker. TableResizeRenderer new (border coordinate calculation + hitTest + marker rendering). InputHandler onMouseMove() extended (RAF throttle + cellBboxes caching). 3 files ~210 lines changed. 582 tests passed, WASM build success, TS compile success |
| 136 | Table resize feature | Done | Rust resizeTableCells batch WASM API + per-row column position layout (build_row_col_x, per-cell independent width support). Cell selection mode (F5): Ctrl+arrow keyboard resize + mouse drag resize + neighbor cell compensation (row width maintained). Normal mode: border drag for column/row full resize. Cell size change triggers text reflow (reflow_cell_paragraph) + cell selection block update. 3 files ~350 lines changed. 582 tests passed, WASM build success, TS compile success |
| 137 | Table keyboard/mouse movement + Undo | Done | Rust moveTableOffset WASM API (offset move + treat_as_char paragraph boundary exchange loop). Layout engine treat_as_char table h/v offset applied. Table object selection mode arrow key movement (grid size units) + mouse drag movement. Table outer click selection. Grid settings dialog. MoveTableCommand Undo/Redo (keyboard merge + drag batch). 9 files ~505 lines changed. 582 tests passed, WASM build success, TS compile success |
| 138 | Image insertion feature | Done | Rust insertPicture WASM API (BinData add + Picture control creation + paragraph insertion). Mouse area designation mode (crosshair cursor + drag rectangle -> size-specified insertion). Click-only uses original 100% size (proportional shrink if exceeds column width). Escape cancel. Accessible from both Insert menu and toolbar. 5 files ~425 lines changed. 582 tests passed, WASM build success, TS compile success |

## Backlog

| No | Description | Found | Status | Notes |
|----|-------------|-------|--------|-------|
| B-006 | Multi-column editing design (pagination + DocumentPosition extension) | 2026-02-18 | Unresolved | Currently multi-column supports rendering only. For editing: line-level column separation (PartialParagraph), column_index in DocumentPosition, cursor/selection column awareness needed |
| B-007 | Shadow text area editing/saving error | 2026-02-21 | Unresolved | Adding characters to shadow-effect text then saving causes error. Possible mismatch between changed bit positions (bits 8-10/11-12/13/14) and existing raw_data during serialization |
| B-008 | ~~File corruption when saving after text input + table creation~~ | 2026-02-21 | Resolved | Fixed in Task 133. split_at() raw_header_extra lost + char_count control not included + empty paragraph has_para_text handling |
