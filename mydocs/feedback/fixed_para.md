현재 문제가 되는 페이지의 레이아웃 처리에서 
1. 표가 나올 때 표를 렌더링하면 표 높이가 더해짐. 이때 표의 문단 시작 점 임시 저장.
2. 이후 문단이 나오면 문단의 줄간격 속성이 '고정 값' 인 경우 이전 임시저장된 y 에서 다음 문단 줄간격부터 임시 변수(fix_vpos_tmp)에 추가하고, 
3. 문단의 줄간격 속성이 '글자에 따라' 가 나오기 전까지 fix_vpos_tmp 에 더해줌.
4. fix_vpos_tmp 의 높이가 1에서 출력한 표 높이보다 클 경우 vpos 에 추가, 아니면 다음 줄간격 속성이 '글자에 따라' 가 나올 때는 표 높이 다음 문단 렌더링